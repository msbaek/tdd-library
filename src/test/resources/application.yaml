spring:
  decorator:
    datasource:
      p6spy:
        enable-logging: true
  jpa:
    hibernate:
      ddl-auto: create
  docker:
    compose:
      lifecycle-management: start_only
      skip:
        in-tests: false